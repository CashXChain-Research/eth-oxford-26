# Runtime dependencies (lean — no dev tools, no test-only packages)
python-dotenv
httpx
numpy

# D-Wave QUBO solver (simulated annealing — runs locally, no QPU needed)
dimod
dwave-neal

# GARCH volatility forecasting (Financial Engineering)
arch

# Agent orchestration
langgraph>=0.2.0
langchain-core>=0.3.0

# API server
fastapi
uvicorn[standard]
pydantic
